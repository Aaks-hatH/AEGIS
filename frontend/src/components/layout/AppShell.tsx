import { Activity, Ambulance, BarChart3, ClipboardList, FileClock, LayoutDashboard, LogOut, Search, Settings, Users } from "lucide-react";
import { Link, NavLink, Outlet, useNavigate } from "react-router-dom";
import { motion } from "framer-motion";
import { useAuth } from "@/contexts/AuthContext";
const nav = [
  { to: "/", label: "Command Center", icon: LayoutDashboard, permission: "analytics:read" }, { to: "/queue", label: "Live Queue", icon: Activity, permission: "queue:read" }, { to: "/ambulances", label: "Ambulances", icon: Ambulance, permission: "ambulances:read" }, { to: "/patients", label: "Patients", icon: Users, permission: "patients:read" }, { to: "/analytics", label: "Analytics", icon: BarChart3, permission: "analytics:read" }, { to: "/audit", label: "Audit Log", icon: FileClock, permission: "audit:read" }, { to: "/settings", label: "Settings", icon: Settings, permission: "settings:read" }
];
export function AppShell() { const { user, can, logout } = useAuth(); const navigate = useNavigate(); return <div className="min-h-screen bg-[radial-gradient(circle_at_top_left,rgba(14,165,233,.16),transparent_28%),hsl(var(--background))]"><aside className="fixed inset-y-0 left-0 hidden w-72 border-r border-slate-200/70 bg-white/75 p-5 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/80 lg:block"><Link to="/" className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-sky-700 font-black text-white">A</div><div><div className="text-lg font-black tracking-tight">AEGIS</div><div className="text-xs uppercase tracking-[.22em] text-slate-500">Command Layer</div></div></Link><nav className="mt-9 grid gap-1">{nav.filter(i => can(i.permission)).map(item => <NavLink key={item.to} to={item.to} className={({ isActive }) => `flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold ${isActive ? "bg-sky-700 text-white" : "text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-900"}`}><item.icon className="h-4 w-4" />{item.label}</NavLink>)}</nav><div className="absolute bottom-5 left-5 right-5 rounded-2xl border border-slate-200 p-4 dark:border-slate-800"><div className="text-sm font-bold">ACUITY Active</div><p className="mt-1 text-xs text-slate-500">Decision support for triage, queue ranking, and pre-arrival preparation.</p></div></aside><main className="lg:pl-72"><header className="sticky top-0 z-20 border-b border-slate-200/70 bg-white/70 px-5 py-4 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/70"><div className="flex items-center justify-between gap-4"><div><div className="text-sm font-bold text-slate-500">Northstar Medical Center</div><div className="text-xl font-black">Emergency Department Operations</div></div><div className="hidden max-w-xl flex-1 items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 dark:border-slate-800 md:flex"><Search className="h-4 w-4 text-slate-400" /><input className="w-full bg-transparent text-sm outline-none" placeholder="Search patients, units, reports, or audit events" /></div><div className="flex items-center gap-3"><div className="text-right"><div className="text-sm font-bold">{user?.name}</div><div className="text-xs uppercase text-slate-500">{user?.role}</div></div><button className="btn btn-secondary" onClick={() => logout().then(() => navigate("/login"))}><LogOut className="h-4 w-4" /></button></div></div></header><motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className="p-5 lg:p-8"><Outlet /></motion.div></main></div>; }

import { Area, AreaChart, Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";import { endpoints } from "@/lib/api";import { useAsync } from "@/lib/useAsync";import { Card } from "@/components/ui/Primitives";
export default function Analytics(){const cap=useAsync(endpoints.capacity,[]);const arr=useAsync(endpoints.arrivals,[]);const queue=useAsync(endpoints.analyticsQueue,[]);if(cap.loading||arr.loading||queue.loading)return <div>Loading analytics...</div>;const capacity=[...(cap.data||[])].reverse().map((m:any)=>({time:new Date(m.measuredAt).toLocaleTimeString([], {hour:"2-digit"}),capacity:m.capacityUtilization,wait:m.averageWaitMinutes,throughput:m.throughputPerHour}));const arrivals=(arr.data?.patients||[]).map((p:any)=>({date:p._id,patients:p.count,ambulances:arr.data.ambulances?.find((a:any)=>a._id===p._id)?.count||0}));return <div className="grid gap-6"><h1 className="text-3xl font-black">Operations Analytics</h1><div className="grid gap-6 xl:grid-cols-2"><Card><h2 className="font-black">Capacity and wait trends</h2><div className="h-80"><ResponsiveContainer><AreaChart data={capacity}><XAxis dataKey="time"/><YAxis/><Tooltip/><Area dataKey="capacity" stroke="#f59e0b" fill="#f59e0b44"/><Area dataKey="wait" stroke="#38bdf8" fill="#38bdf844"/></AreaChart></ResponsiveContainer></div></Card><Card><h2 className="font-black">Arrivals by day</h2><div className="h-80"><ResponsiveContainer><BarChart data={arrivals}><XAxis dataKey="date"/><YAxis/><Tooltip/><Bar dataKey="patients" fill="#38bdf8"/><Bar dataKey="ambulances" fill="#f97316"/></BarChart></ResponsiveContainer></div></Card></div><Card><h2 className="font-black">Queue movement patterns</h2><div className="mt-4 grid gap-2">{queue.data?.movementPatterns?.map((m:any)=><div key={m._id} className="flex justify-between rounded-xl border border-slate-200 p-3 dark:border-slate-800"><span>{m._id}</span><b>{m.count}</b></div>)}</div></Card></div>}

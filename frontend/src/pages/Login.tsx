import { useState } from "react";import { useNavigate } from "react-router-dom";import { useAuth } from "@/contexts/AuthContext";import { Card, Field } from "@/components/ui/Primitives";
export default function Login(){const{login}=useAuth();const nav=useNavigate();const[error,setError]=useState("");const[email,setEmail]=useState("admin@aegis.health");const[password,setPassword]=useState("AegisSecure123!");return <div className="grid min-h-screen place-items-center bg-slate-950 px-4 text-white"><Card className="w-full max-w-md border-slate-800 bg-slate-900/90"><div className="mb-8"><div className="text-3xl font-black">AEGIS</div><p className="mt-2 text-sm text-slate-400">Secure access to emergency operations and ACUITY decision support.</p></div><form className="grid gap-4" onSubmit={async e=>{e.preventDefault();setError("");try{await login(email,password);nav("/")}catch(err:any){setError(err.message)}}}><Field label="Email"><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/></Field><Field label="Password"><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></Field>{error&&<div className="rounded-xl border border-red-900 bg-red-950/40 p-3 text-sm text-red-200">{error}</div>}<button className="btn btn-primary" type="submit">Sign in</button></form></Card></div>}

import { endpoints } from "@/lib/api";import { useAsync } from "@/lib/useAsync";import { Card } from "@/components/ui/Primitives";import { fmtDate } from "@/lib/utils";
export default function Audit(){const{data,loading}=useAsync(endpoints.audit,[]);if(loading)return <div>Loading audit trail...</div>;return <div className="grid gap-6"><h1 className="text-3xl font-black">Audit and Governance</h1><Card><table className="w-full text-left text-sm"><thead className="text-xs uppercase text-slate-500"><tr><th>Time</th><th>Actor</th><th>Action</th><th>Resource</th><th>Status</th></tr></thead><tbody>{data?.map((l:any)=><tr key={l._id} className="border-t border-slate-200 dark:border-slate-800"><td className="py-3">{fmtDate(l.createdAt)}</td><td>{l.actorEmail||l.actor?.email}</td><td className="font-bold">{l.action}</td><td>{l.resourceType}</td><td>{l.status}</td></tr>)}</tbody></table></Card></div>}

import { useEffect, useState } from "react";import { endpoints } from "@/lib/api";import { useAsync } from "@/lib/useAsync";import { Card, Field } from "@/components/ui/Primitives";
export default function Settings(){const{data,loading,reload}=useAsync(endpoints.settings,[]);const users=useAsync(endpoints.users,[]);const[form,setForm]=useState<any>({});useEffect(()=>{if(data)setForm(data)},[data]);if(loading)return <div>Loading settings...</div>;return <div className="grid gap-6"><h1 className="text-3xl font-black">Settings and Configuration</h1><div className="grid gap-6 xl:grid-cols-2"><Card><h2 className="font-black">Hospital metadata and policy</h2><div className="mt-4 grid gap-3"><Field label="Hospital name"><input className="input" value={form.hospitalName||""} onChange={e=>setForm({...form,hospitalName:e.target.value})}/></Field><Field label="Unit name"><input className="input" value={form.unitName||""} onChange={e=>setForm({...form,unitName:e.target.value})}/></Field><Field label="Total beds"><input className="input" type="number" value={form.capacity?.totalBeds||0} onChange={e=>setForm({...form,capacity:{...form.capacity,totalBeds:Number(e.target.value)}})}/></Field><Field label="Theme"><select className="input" value={form.theme||"dark"} onChange={e=>setForm({...form,theme:e.target.value})}><option>dark</option><option>light</option><option>system</option></select></Field><button className="btn btn-primary" onClick={()=>endpoints.saveSettings(form).then(reload)}>Save settings</button></div></Card><Card><h2 className="font-black">User management</h2><div className="mt-4 grid gap-2">{users.data?.map((u:any)=><div key={u._id} className="flex justify-between rounded-xl border border-slate-200 p-3 dark:border-slate-800"><div><b>{u.name}</b><div className="text-sm text-slate-500">{u.email}</div></div><div className="text-sm uppercase text-slate-500">{u.role}</div></div>)}</div></Card></div></div>}
